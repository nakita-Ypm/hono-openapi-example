{
  "name": "hono_openapi_example",
  "private": true,
  "scripts": {
    "fmt": "pnpm biome format --write ./",
    "migrate": "rm -rf packages/prisma/dev.db && pnpm -F @packages/prisma migrate",
    "test": "vitest run"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "vitest": "^2.1.4"
  },
  "dependencies": {
    "@hono/zod-openapi": "^0.18.0"
  }
}
